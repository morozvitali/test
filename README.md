# test
java test basic

⭐ Як правильно робити проєкт для тестових Java-завдань
(прості завдання → прості тести → поступове ускладнення)
Тут ми говоримо не про Spring, а про класичний Java-проєкт для тренувальних задач.
1️⃣ Яку структуру робити?
Найкраще — простий Maven або Gradle проєкт.

Maven структура:
my-tests/
└── src/
├── main/
│     └── java/
│           └── org.example/   ← твої рішення задач
└── test/
└── java/
└── org.example/   ← тести на JUnit

2️⃣ Що підключати? (мінімальний набір)
✔ JUnit 5
Для написання тестів.

✔ AssertJ або стандартні asserts
Найчастіше:
Assertions.assertEquals()
Assertions.assertTrue()
Assertions.assertThrows()

✔ (за бажанням) Mockito
Для найпростіших рівнів — не потрібно. Пізніше — так.

📦 Maven dependencies (мінімум):
<dependencies>
<dependency>
<groupId>org.junit.jupiter</groupId>
<artifactId>junit-jupiter</artifactId>
<version>5.10.0</version>
<scope>test</scope>
</dependency>
</dependencies>

3️⃣ Як повинні виглядати задачі?
Ти створюєш клас із методом, наприклад:
public class Task01 {
public static int add(int a, int b) {
return a + b;
}
}

Поруч — тест:
class Task01Test {
@Test
void testAdd() {
int result = Task01.add(2, 3);
Assertions.assertEquals(5, result);
}
}

4️⃣ Як воно має працювати?

✔ Ти запускаєш тільки тести (IntelliJ → Run Tests)
✔ Вони або проходять, або ні
✔ Якщо падають — виправляєш код
✔ Рухаєшся далі

Це модульна тренувальна система:
кожна задача = один метод + тест.

5️⃣ Чому важливо почати з простих задач?

Бо так ти:
звикаєш до структури
звикаєш до підходу "рішення → тести"
формуєш автоматичну звичку чистого коду
створюєш фундамент, який потім масштабується